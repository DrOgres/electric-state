<div>
    weapons <div class="item-create" data-type="weapon">new weapon</div>
    {{#each items as |item|}}
        {{#if item.isWeapon}}
        {{log item._id}}
            <div>{{item.name}}</div> <div class="item-edit" data-item-id="{{item._id}}">edit</div>
             <div class="item-delete" data-item-id="{{item._id}}">delete</div>
        {{/if}}
    {{/each}}
    explosives
    {{#each items as |item|}}
        {{#if item.isExplosive}}
            <div>{{item.name}}</div>
        {{/if}}
    {{/each}}
    armor
        {{#each items as |item|}}
        {{#if item.isArmor}}
            <div>{{item.name}}</div>
        {{/if}}
    {{/each}}
    gear
     {{#each items as |item|}}
        {{#if item.isGear}}
            <div>{{item.name}}</div>
        {{/if}}
    {{/each}}
    drone
        {{#each items as |item|}}
        {{#if item.isDrone}}
            <div>{{item.name}}</div>
        {{/if}}
    {{/each}}
    neurocaster
        {{#each items as |item|}}
        {{#if item.isNeurocaster}}
            <div>{{item.name}}</div>
        {{/if}}
    {{/each}}
  
</div>