<form class="{{cssClass}} flexcol" autocomplete="off">

  <div class="flexrow">
    <div class="portrait"><img src="{{item.img}}" data-edit="img" title="{{item.name}}" height="160" width="160" />
    </div>
    <div class="content flexcol">
      <h1 class="charname">
        <input name="name" type="text" value="{{item.name}}" />
      </h1>
      <label> Type </label>
      <select name="system.type">
        {{selectOptions config.traitTarget selected=item.system.type localize=true}}
      </select>
      <label>Bonus</label> <input type="number" name="system.modifier.value" value="{{item.system.modifier.value}}">
      <label>Description</label> <textarea name="system.description"
        value="{{item.system.description}}">{{item.system.description}}</textarea>

    </div>
  </div>

</form>